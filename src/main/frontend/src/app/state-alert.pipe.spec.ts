import { StateAlertPipe } from './state-alert.pipe';

describe('StateAlertPipe', () => {
  it('create an instance', () => {
    const pipe = new StateAlertPipe();
    expect(pipe).toBeTruthy();
  });
});
